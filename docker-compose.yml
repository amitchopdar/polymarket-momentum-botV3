version: '3.8'

services:
  polymarket-v3:
    build: .
    container_name: polymarket-bot-v3
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./PolyDB_V3.sqlite:/app/PolyDB_V3.sqlite
